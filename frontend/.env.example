VITE_API_URL=http://localhost:4000/api/v1
# Clave del bot externo de leads (ver src/shared/api/leadsExternos.ts) — pide
# el valor real, no la dejes vacía en producción o el bot rechazará todo con 401.
VITE_EXTERNAL_LEAD_API_KEY=
